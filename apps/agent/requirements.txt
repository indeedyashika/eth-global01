starlette>=0.40.0
uvicorn>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.9
httpx>=0.27.0
# MCP server SDK for mcps/hedera/{read_server,write_server}.py,
# mcps/evm/{read_server,write_server}.py, and mcps/worldid/server.py to
# expose themselves to the Hermes agent as native tools (see
# server.py:write_config_yaml's mcp_servers.hedera_read/hedera_write entries).
# Pinned here rather than relying on hermes-agent's own [all] extras so the
# SDK is present regardless of what upstream bundles.
mcp>=1.2
# websockets is the upstream client our reverse proxy uses to connect to
# hermes's /api/pty, /api/ws, /api/events endpoints. httpx does not speak
# WebSockets, so we need a dedicated client library.
websockets>=12.0
